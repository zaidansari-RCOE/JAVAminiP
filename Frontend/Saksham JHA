<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Tracker (Simple)</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Custom Styles and Font Family */
        body { font-family: 'Inter', sans-serif; background-color: #f0f4f8; }
        /* Note: The custom style rule below seems to have a typo 'cbAd5e1' which might be intended as a color or it's a placeholder. */
        .card { border: 1px solid #cbAd5e1; }
        .btn-green { background-color: #10b981; color: white; }
        .btn-red { background-color: #ef4444; color: white; }
        .btn-indigo { background-color: #6366f1; color: white; }
    </style>
</head>
<body class="p-4 sm:p-8">

    <div class="max-w-4xl mx-auto">
        <h1 class="text-3xl font-bold text-gray-900 text-center mb-8">Expense Tracker</h1>

        <div class="bg-white p-6 rounded-xl shadow-lg mb-8 card">
            <h2 id="form-title" class="text-xl font-semibold mb-4 border-b pb-2">Add New Transaction</h2>
            
            <form id="transaction-form" class="grid grid-cols-1 md:grid-cols-5 gap-4 items-end">
                <input type="hidden" id="transaction-id">

                <div class="md:col-span-2">
                    <label for="description" class="block text-sm font-medium text-gray-700">Expense/Income</label>
                    <input type="text" id="description" required placeholder="e.g., Groceries / Monthly Salary" class="mt-1 w-full px-3 py-2 border border-gray-300 rounded-md">
                </div>
                
                <div>
                    <label for="category" class="block text-sm font-medium text-gray-700">Category</label>
                    <select id="category" class="mt-1 w-full px-3 py-2 border border-gray-300 rounded-md">
                        <option value="Food">Food</option>
                        <option value="Transport">Transport</option>
                        <option value="Utilities">Utilities</option>
                        <option value="Health">Health</option>
                        <option value="Entertainment">Entertainment</option>
                        <option value="Shopping">Shopping</option>
                        <option value="Housing">Housing</option>
                        <option value="Other">Other</option>
                    </select>
                </div>

                <div>
                    <label for="amount" class="block text-sm font-medium text-gray-700">Price</label>
                    <input type="number" id="amount" required min="0.01" step="0.01" placeholder="20" class="mt-1 w-full px-3 py-2 border border-gray-300 rounded-md">
                </div>

                <div>
                    <label for="date" class="block text-sm font-medium text-gray-700">Date</label>
                    <input type="date" id="date" required class="mt-1 w-full px-3 py-2 border border-gray-300 rounded-md">
                </div>
                
                <div id="action-buttons-container" class="md:col-span-5 flex gap-4 mt-4">
                    <button type="submit" data-type="expense" id="expense-btn" class="btn-red flex-1 py-2 rounded-lg font-semibold shadow-md hover:opacity-90">ADD EXPENSE</button>
                    <button type="submit" data-type="income" id="income-btn" class="btn-green flex-1 py-2 rounded-lg font-semibold shadow-md hover:opacity-90">ADD INCOME</button>
                    <button type="button" id="clear-form-btn" class="hidden py-2 px-4 text-sm font-medium text-gray-700 bg-gray-200 hover:bg-gray-300 rounded-md shadow-md">Clear Form</button>
                </div>
            </form>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-8">
            <div class="bg-white p-4 rounded-xl shadow-md text-center card">
                <h3 class="text-sm font-medium text-gray-600">Total Income</h3>
                <p id="total-income" class="text-2xl font-bold text-green-600 mt-1">₹0.00</p>
            </div>
            <div class="bg-white p-4 rounded-xl shadow-md text-center card">
                <h3 class="text-sm font-medium text-gray-600">Total Expenses</h3>
                <p id="total-expense" class="text-2xl font-bold text-red-600 mt-1">₹0.00</p>
            </div>
            <div class="bg-white p-4 rounded-xl shadow-md text-center card">
                <h3 class="text-sm font-medium text-gray-600">Net Balance</h3>
                <p id="net-balance" class="text-2xl font-bold text-indigo-600 mt-1">₹0.00</p>
            </div>
        </div>

        <div class="bg-white p-6 rounded-xl shadow-lg card">
            <h2 class="text-xl font-semibold mb-4 border-b pb-2">Table #1 Transaction History</h2>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead>
                        <tr class="text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            <th class="px-4 py-3">No.</th>
                            <th class="px-4 py-3">Type</th>
                            <th class="px-4 py-3">Expense/Income</th>
                            <th class="px-4 py-3">Category</th>
                            <th class="px-4 py-3">Price</th>
                            <th class="px-4 py-3">Date</th>
                            <th class="px-4 py-3 text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="transaction-list" class="bg-white divide-y divide-gray-200">
                        </tbody>
                </table>
            </div>
        </div>
        
    </div>
</body>
</html>
